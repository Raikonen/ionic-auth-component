import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'reusable-login-component',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
